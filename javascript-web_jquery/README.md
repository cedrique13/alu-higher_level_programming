# 📁 javascript-web_jquery

> JavaScript DOM & jQuery — Holberton ALU Project

---

## 📚 Description

This project is a hands-on introduction to **DOM manipulation** and **jQuery**, a fast, small, and feature-rich JavaScript library. It covers essential front-end techniques like changing element styles, modifying the DOM dynamically, handling events, and making HTTP requests using `jQuery.ajax()`.

The goal is to gain confidence using JavaScript to interact with web pages without reloading them — building responsive and interactive user experiences.

---

## 🧠 Learning Objectives

By completing this project, I am able to:

- Understand why jQuery makes front-end programming easier.
- Select HTML elements using both vanilla JavaScript and jQuery.
- Differentiate between ID, class, and tag name selectors.
- Modify HTML element styles and content.
- Bind and handle user and DOM events.
- Perform asynchronous `GET` and `POST` requests using jQuery AJAX.
- Manipulate the DOM without reloading the page.

---

## 🛠️ Technologies Used

- JavaScript (ES6+)
- jQuery v3.2.1
- HTML5
- Chrome 57.0 for testing
- Code editor: `vim`, `emacs`, or `Visual Studio Code`

---
